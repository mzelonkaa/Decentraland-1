/// <reference types="env" />
/**
 * Sign a message with your address
 *
 * @param messageToSign String to sign
 * @returns Initial message and its signature
 */
export declare function signMessage(messageToSign: string): Promise<{
    message: string;
    signature: string;
}>;
