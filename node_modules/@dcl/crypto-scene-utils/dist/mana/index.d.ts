/// <reference types="env" />
import * as eth from 'eth-connect';
/**
 * Send MANA to an address
 *
 * @param toAddress Receiver address
 * @param amount Amount in ether to send
 * @param waitConfirm Resolve promise when tx is mined or not
 */
export declare function send(toAddress: eth.Address, amount: number, waitConfirm?: boolean): Promise<any>;
/**
 * Return the balance of the current user
 */
export declare function myBalance(): Promise<import("../utils/types").TransactionObject<string>>;
/**
 * Return the balance of the address
 *
 * @param address Address you are checking
 */
export declare function balance(address: eth.Address): Promise<import("../utils/types").TransactionObject<string>>;
